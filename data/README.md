# Catalog reference data

| File | Purpose |
|------|---------|
| `final_keml_2023.json` | KEML catalog seed (`npm run db:seed`) |
| `final_keml_2023.csv` | Same data for spreadsheets |
| `alias_names.json` | Kenyan brand aliases (`npm run db:seed-aliases`) |
| `clean_index_names.json` | Legacy index names (not seeded) |
| `clean_index_names_with_pages.json` | PDF page cross-reference |
